{
  "name": "portal-hospital-v2-functions",
  "private": true,
  "description": "Cloud Functions V2 — Portal Hospital (callables login + health)",
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "scripts": {
    "serve": "node -e \"console.log('Las funciones se prueban contra Cloud Functions desplegadas (sin emulador local). Usá: npm run firebase:deploy:functions desde la raíz del repo.'); process.exit(0)\"",
    "lint": "echo \"Sin ESLint configurado aún\""
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.2.5",
    "ulid": "^2.3.0"
  }
}
