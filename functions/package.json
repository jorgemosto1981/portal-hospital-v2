{
  "name": "portal-hospital-v2-functions",
  "private": true,
  "description": "Cloud Functions V2 — Portal Hospital (callable health + syncSessionClaims stub)",
  "engines": {
    "node": ">=20"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "lint": "echo \"Sin ESLint configurado aún\""
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^6.3.2"
  }
}
