/**
 * Filtros de elegibilidad (`filtros_elegibilidad`).
 * Recibe `data`, `update`, `errors` desde el orquestador (stub).
 */
export default function ElegibilidadTab() {
  return (
    <div className="space-y-2 text-base text-slate-700">
      <p className="text-sm text-slate-500">
        Elegibilidad por escalafón, efector, vínculo, etc. (stub).
      </p>
    </div>
  );
}
