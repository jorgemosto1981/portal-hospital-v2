/**
 * Parámetros normativos y plazos (días legales/internos, tipo de cómputo).
 * Recibe `data`, `update`, `errors` desde el orquestador (stub).
 */
export default function PlazosTab() {
  return (
    <div className="space-y-2 text-base text-slate-700">
      <p className="text-sm text-slate-500">
        Plazos y cómputo (`plazo_documental_*`, `cfg_tcp_*`). Stub.
      </p>
    </div>
  );
}
