/**
 * Reglas de workflow (aprobaciones, remanentes, reemplazo).
 * Recibe `data`, `update`, `errors` desde el orquestador (stub).
 */
export default function WorkflowTab() {
  return (
    <div className="space-y-2 text-base text-slate-700">
      <p className="text-sm text-slate-500">
        Workflow y políticas de solicitud (stub).
      </p>
    </div>
  );
}
