# Logs
logs
*.log
npm-debug.log*

# Dependencies
node_modules/
dist/
dist-ssr/

# Local env
.env
.env.*.local
.env.local
*.local

# Firebase
.firebase/
firebase-debug.log

# Service accounts (never commit)
serviceAccountKey.json
*-service-account*.json
# Claves JSON descargadas desde GCP (patrón típico en la raíz de este repo)
portal-hospital-v2-*.json

# Copias de seguridad locales (no subir ZIPs)
backups/*.zip

# OS
.DS_Store
Thumbs.db
